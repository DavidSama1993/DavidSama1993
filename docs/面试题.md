# 面试题

## HTML
1. 含有src属性的标签：img, script, iframe, audio, video
2. HTML5标签：
   + 图像：canvas
   + 新多媒体元素：audio, video, source, embed, track
   + 语义结构标签：article, aside, details, header, footer, section, nav
3. 块级元素：div, h1-h6, p, ul, ol, li（旧元素）；canvas, article, header, footer, section, nav...（html5元素）
4. 行内元素：span, input, textarea, img...

## CSS
1. 行内元素和块级元素区别
2. 你了解的布局系统有哪些
   + 流式布局（默认布局，经典盒模型）width, height, padding, border, margin;
   + 浮动布局 float
   + 定位 position: static | relative | absolute | fixed;
   + CSS3位移 transform
   + 弹性盒模型 display:flex;
   + 网格布局 display:grid;（这个我也不了解）
     ***元素居中方案***
3. box-sizing两个属性值是什么，作用分别是什么：content-box(元素宽高就是width和height定义的宽高), border-box(元素宽高包含padding和border的宽度)
4. 清除浮动方式
   + 在容器元素增加一个空的块级元素
   + 通过伪元素::after清除浮动
   + 通过触发容器元素BFC包裹内容元素
5. BFC相关
   + 概念：BFC 即 Block Formatting Contexts (块级格式化上下文)，是W3C CSS2.1规范中的一个概念，决定了元素如何对其内容进行定位，以及与其他元素的关系和相互作用。具有BFC特性的元素可以看做是隔离了的独立容器，容器里面的元素不会在布局上影响到外面的元素，并且BFC具有普通容器所没有的的一些特性。
   + BFC生成条件：
      + body 根元素
      + 浮动元素：float 除 none 以外的值
      + 绝对定位元素：position (absolute、fixed)
      + display 为 inline-block、table-cells、flex
      + overflow 除了 visible 以外的值(hidden、auto、scroll)
6. z-index优先级比较
   + 基本概念 
      + z-index是深度属性，设置元素在z轴上面的堆叠顺序。
      + 只有dom设置了position:absolute | relative | fixed 才会有z-index属性
      + 堆叠上下文：当前dom往上级找，如果该级设置了z-index属性值(非auto)，那么该级别dom就是堆叠上下文；如果往上级一直找不到，那么根级别dom就是堆叠上下文。
   + 比较规则
      1. 有position属性的dom(脱离文档流的dom)会在没有position属性的dom(文档流内的dom)的上面。
      2. 文档流内容的堆叠遵循后来居上的原则。(后面的如果和前面的dom重叠了，后面盖在前面)
      3. 同级的dom，它们的堆叠上下文一定相同。所以直接z-index比较，大的在上面，小的在下面，相同的遵循后来居上的原则。
      4. 非同级的dom，先要看它们的堆叠上下文。如果堆叠上下文一致，那么比较同(3)；如果不一致，就比较他们堆叠上下文的z-index。(dom的z-index受父级的约束，父级如果小的话，子级再大也没用!)
7. 弹性盒布局属性
   + 弹性盒容器元素
      + display:flex; 
      + 方向和换行 flex-flow: row | column, wrap | nowrap; 可拆分为flex-direction和flex-wrap两个属性
      + 主轴对齐方式 justify-content: flex-start | flex-end | center | stretch | space-between | space-around;
      + 交叉轴对齐方式 align-items: flex-start | flex-end | center | stretch;
      + 因换行导致多条主轴的对齐方式 align-content
   + 弹性子元素
8. （了解性问题）碰到一些特殊的日子，比如全国哀悼日，公司要求把网站整体改成黑白的应该怎么设置？ html { filter: grayscale(1); }
9. （了解性问题）BEM命名规范（block-element-modifier通过模块化思想解决class命名问题, 将页面UI看做一个个组件，将元素通过固定的结构命名，例如el-input__wrap）
10. （了解性问题）atom-css（将固定样式分解为类名，例如 .mg-10 { margin: 10px } ）

## JS
1. 数据类型：
   + 基本数据类型：Boolean, Number, String, null, undefined，以及es6新增的Symbol
   + 复合类型: Object
   + 内置对象 Boolean, Number, String, Function, Array, JSON, Math, Regexp, Date, Map, Set, Promise...
   + 类型判断方法： typeof操作符，instanceof操作符，Object.prototype.toString.call()
2. DOM操作方法
   + 获取元素
      + id查询 getElementById()
      + 类名查询 getElementsByClassName()
      + 标签名称查询 getElementsByTagName()
      + css选择器查询首个元素 querySelector()
      + css选择器查询所有元素 querySelectorAll()
   + 插入元素
      + 在当前元素内末尾插入元素 appendChild()
   + 删除元素
      + 删除元素内指定子元素 removeChild()
   + 查询属性
      + getAttribute()
   + 修改属性
      + setAttribute()
3. 冒泡排序
4. cookie操作
   + document.cookie
5. 跨域概念，原因，解决方案
6. window.location对象下都有哪些属性
   + eg. https://www.baidu.com/s?ie=utf-8&f=8&rsv_bp=1&rsv_idx=1&tn=baidu&wd=dom%E6%93%8D%E4%BD%9C%E6%96%B9%E6%B3%95&fenlei=256&oq=atomcss&rsv_pq=830416f800093583&rsv_t=62babVOve9xDLhJJbn9XakSo92wPyCw2rnuI%2BJThv9n%2Bk1S7jXHSgsnrb2Q&rqlang=cn&rsv_enter=1&rsv_dl=tb&rsv_btype=t&inputT=3616&rsv_sug3=170&rsv_sug1=160&rsv_sug7=100&bs=atomcss#asdasd/asasd/asdfgdf?ididididi=1
      + hash: "#asdasd/asasd/asdfgdf?ididididi=1"
      + host: "www.baidu.com"
      + hostname: "www.baidu.com"
      + href: "https://www.baidu.com/s?ie=utf-8&f=8&rsv_bp=1&rsv_idx=1&tn=baidu&wd=dom%E6%93%8D%E4%BD%9C%E6%96%B9%E6%B3%95&fenlei=256&oq=atomcss&rsv_pq=830416f800093583&rsv_t=62babVOve9xDLhJJbn9XakSo92wPyCw2rnuI%2BJThv9n%2Bk1S7jXHSgsnrb2Q&rqlang=cn&rsv_enter=1&rsv_dl=tb&rsv_btype=t&inputT=3616&rsv_sug3=170&rsv_sug1=160&rsv_sug7=100&bs=atomcss"
      + origin: "https://www.baidu.com"
      + pathname: "/s"
      + port: ""
      + protocol: "https:"
      + search: "?ie=utf-8&f=8&rsv_bp=1&rsv_idx=1&tn=baidu&wd=dom%E6%93%8D%E4%BD%9C%E6%96%B9%E6%B3%95&fenlei=256&oq=atomcss&rsv_pq=830416f800093583&rsv_t=62babVOve9xDLhJJbn9XakSo92wPyCw2rnuI%2BJThv9n%2Bk1S7jXHSgsnrb2Q&rqlang=cn&rsv_enter=1&rsv_dl=tb&rsv_btype=t&inputT=3616&rsv_sug3=170&rsv_sug1=160&rsv_sug7=100&bs=atomcss"
7. 事件
   + 事件冒泡(从下到上)
   + 事件捕获(从上到下)
   + Event.target和Event.currentTarget的区别(currentTarget指向事件绑定的元素，target指向事件触发的元素)
   + 事件代理(利用事件冒泡原理，由父级元素代为处理子元素事件，然后通过event.target区分不同子元素执行不同的逻辑)
8. 函数的this指向（普通函数/箭头函数）
9. 闭包的概念
10. js原型链的概念
11. js模块：cmd，amd, commonjs, es6模块
12. es6相关
   + 变量声明 let、const和var的区别
   + 变量解构赋值
   + 数组的方法： filter, map, forEach, includes, some, find, findIndex, every
   + Set
   + Map
   + 异步函数
   + Promise
   + Class语法，继承
13. 对TS的了解

## vue
1. vue指令：v-if, v-else, v-else-if, v-show, v-model, v-bind, v-for
   + v-if和v-show的区别
2. 事件处理
   + 原生dom元素事件处理
   + 自定义组件事件处理
   + 事件修饰符 .stop, .prevent, .native
3. 生命周期钩子函数
   + 内容： beforeCreate, created, beforeMount, mounted, beforeUpdate, updated, beforeDestroy, destroyed
   + 执行条件，父子组件生命周期执行顺序: 父组件created -> 子组件created -> 子组件mounted -> 父组件mounted
4. 父子组件传参
   + 父传子：通过子组件的prop属性定义传入参数
   + 子传父：通过在子组件内定义自定义事件，父组件监听自定义事件处理传出的值
5. vue双向绑定原理： 通过Object.defineProperty进行数据劫持（点到为止）
6. computed和watch的概念和区别
7. vue中对数组类型变量赋值应该注意什么？ 变异性方法可以直接操作数组，非变异性方法需要用返回值替换给原来的变量
   + 变异性方法（会改变原始数组的方法）sort, reverse, push, pop, shift, unshift, splice
   + 非变异性方法（返回一个新数组的方法，纯函数方法）slice, concat, filter, map等
8. $set, $delete的作用是什么，为什么会有这两个方法？
   + $set添加一个响应式数据，$delete删除一个响应式数据
   + 因为vue是在初始化实例的时候将data内定义的变量通过defineProperty设置为响应式数据的，而实例化之后通过"."操作符添加的数据vue无法对其进行双向绑定，通过delete关键字删除某个字段也无法动态监听，所以vue提供了这两个api来进行动态添加或删除字段
9. vue-router中的$route和$router有什么区别
   + $route是当前路由信息对象，包含path, params, query, name, fullPath等
   + $router是路由实例对象
10. vuex的作用，state、mutation、action、module的概念是什么，怎么用?
11. vue项目中如何解决跨域问题？ 在vue.config.js中设置代理
12. \<style scoped>中的scoped作用是什么？会导致什么问题？怎么解决？
   + 概念：在style标签中设置scoped会在生成的样式选择器后面添加\[data-hash]属性，对应本组件生成的dom元素，起到限制样式作用域的作用，避免组件之间的样式污染
   + 导致的问题：由于vue只会在组件模板直接声明的dom元素和其使用的自定义组件的最外层dom元素上添加\[data-hash]属性，所以在父组件中设置子组件的样式会出现选择器不生效的问题
   + 解决方案：使用脚手架提供的/deep/或::v-deep深度选择器操作子组件dom元素的样式

## 其他
1. git使用
2. 项目经验（处理过什么问题）